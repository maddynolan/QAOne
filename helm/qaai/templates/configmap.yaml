apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "qaai.fullname" . }}-config
  labels:
    {{- include "qaai.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.backend.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
